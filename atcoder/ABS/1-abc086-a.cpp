#include <bits/stdc++.h>

// using
using namespace std;

// input 
int a,b;

int main() {
  cin >> a >> b;
  if ((a % 2) == 0 || (b % 2) == 0) cout << "Even" << endl;
  else cout << "Odd" << endl;
  return 0;
}
